const enums = require('~/app/enums')

export const state = () => ({
  categories: enums.categories,
  widthType: enums.widthType,
})


export const mutations = {
  // no mutations possible, this is readonly
}

export const actions = {
  //no actions possible, this is readonly
}